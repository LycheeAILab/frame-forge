# 生图后端

## 用户内置工具

查看当前工具列表和 schema，使用实际可调用的生图工具。内置不要求 OPENAI_API_KEY，不访问自有服务器，也不读取或导出 Codex 登录凭据。工具未暴露模型选择时不能硬填 gpt-image-2，也不能声称已确认其底层模型。

按工具文档传递真实参考图片。本地图片先检视再传图；本宿主可用 referenced_image_paths 或 num_last_images_to_include，不能同时传两者，其他宿主按实际 schema 映射。纯文字生图不传示例图。

超时、限流、任务进行中或结果未知时保留原任务，不另起后端重复提交。内容拒绝不触发服务切换。

## 自有 imageapi（待接入）

未收到接口文档，当前不能运行此路径。保留选中风格、最终提示词和输入清单，明确告知接口尚未配置。不要猜测端点、模型名、请求字段或响应格式。

接入需提供：服务文档或代码路径；用户登录/鉴权；模型与尺寸；文生图/图生图、上传格式；同步响应或异步任务查询/下载；计费与重试、幂等机制。

适配器只负责鉴权、传图、提交、查询及下载，接收完整提示词，不重新选择风格。使用每位用户凭据或短期令牌，不将共享服务端密钥打包进 skill；不把密钥写入提示词、日志或样例。用户选择服务后按已有授权继续；缺凭据时走已核实的登录流程，不让用户在聊天中发密钥。
