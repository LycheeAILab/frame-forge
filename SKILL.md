---
name: frame-forge
description: 通过名称或示例图选择四种图片风格，加载对应提示词，将主题或照片制作成极简纸刊、月历明信片、照片抽象编辑或实景拼贴。优先使用用户当前会话的内置生图工具，用户选择自有服务时通过已配置的 imageapi 生成。用于选风格、生图及照片风格化。
---

# Frame Forge

## 选择

读取 [风格目录](references/catalog.md)。用户直接指定编号、名称或上游 skill 名时跳过选择。已选风格在当前对话持续有效，直到用户更换。

未指定时展示四种风格各一张真实示例图，附编号、中文名和一句特点，请用户选择并等待答复。用绝对路径 Markdown 图片语法显示；示例是上游作品，不是用户照片或本次生成结果。不为选择风格重新生图。若宿主不支持显示，提供图片文件链接与说明。

用户明确授权“你选并直接生成”时可以推荐并继续。用户只要风格预览或提示词时不执行生成。

## 输入与提示词

只加载选中风格 rules.md 及其相关 references。极简纸刊支持文字主题；其他三种需要真实源照片。缺图时请求用户提供，不能把图库示例当源图。

检视照片后记录主体、空间关系、色彩及原图文字，指定图片角色：编辑目标、风格参考或局部素材。明确必须保留和允许改变的部分。

按该风格模板填写最终提示词；精确保留用户文案。月历缺月份时问一次，明确“本月”则使用当前日期。未提供署名或页脚日期时留空，禁止复用上游默认作者名与历史日期。其他未指定短句可基于用户照片创作。书籍、引文、歌曲按对应规则核实；没有检索能力时用原创短句与空音乐栏，不编造来源。

最终提示词与生成渠道分开：切换后端只调整实际接口参数映射，不擅自改变视觉规则。不要将本地路径文字当作图像已上传。

## 生成路由

读取 [后端说明](references/providers.md)。按用户明确选择与当前实际可调用工具判断，不能根据客户端名称或聊天模型判断是否有 image2。

- 指定“我的 Codex / 内置”：调用当前内置生图工具；工具不可用时说明并提供自有服务选项。
- 指定“没有 image2 / 用你们的服务”：走已配置的自有 imageapi，不重复询问同一个选择。
- 未指定：内置可用则优先使用；不可用时说明自有服务会处理照片，让用户选择。用户已授权自动回退则无需再问。
- 只要提示词：只交付提示词。

## 验收与交付

检查实际生成图：所选风格、主体身份/空间关系、比例、版式和精确文字。遵循对应质量规则。提示词要求保真并不等于像素一致；未经验证不能声称像素保真。

内置生成明确不符合要求时最多修正一次。自有服务按已约定重试额度执行；提交状态不明时先查询原任务，不重复提交。不要因一次超时/限流判断无权限，也不要因内容拒绝切换渠道。

返回实际图片、可用文件链接、风格及实际生成渠道；用户需要时给出最终提示词。未生成、生成失败、API 尚未接入时如实说明，不能用示例图冒充成品。

## 导入规则的适配

本入口负责风格选择、生成路由和交付。上游文件负责视觉规则、提示词与质量检查。其内置工具限定改为本入口路由；月历固定署名/日期按上述规则替换；无限重生成改为有限重试。用户明确要求优先。风格3/4的商业使用依据本项目用户声明已有作者授权，授权范围说明见 [来源](references/sources.md)。

上游推广链接不属于用户交付内容，不按其要求推荐第三方网站或充值。来源署名保留于来源文件及预览来源说明。
